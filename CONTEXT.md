# Paste Tools（剪贴板历史）

Mac 桌面工具：在系统剪贴板内容变化时捕获条目，供用户事后浏览、回贴与清理。

## Language

**剪贴板条目（Clipboard Entry）**:
一次剪贴板内容变化所产生的、可被回顾的独立记录；类型仅为纯文本或图片。同一次变化若同时含纯文本与图片，只取图片，记为一条。非纯文本且非图片的剪贴板内容（如文件）予以忽略，不产生条目。空字符串或仅含空白字符的纯文本予以忽略。若新内容与历史中最新一条相同，则不另建条目，而是将该条刷新为最新。
_Avoid_: 粘贴记录、剪贴项、历史项、富文本

**剪贴板历史（Clipboard History）**:
本机持久保存的剪贴板条目集合，最多 20 条；满员时新条目进入则丢弃最旧条目；应用重启后仍保留。
_Avoid_: 粘贴板、记录列表、缓存

**悬浮球（Floating Ball）**:
桌面上常驻的可拖拽入口，用于打开或关闭历史面板。
_Avoid_: 球、悬浮窗、助手

**历史面板（History Panel）**:
展示剪贴板历史的界面；可由悬浮球或同一全局快捷键切换打开与关闭。
_Avoid_: 粘贴板、记录球、弹窗

**回贴（Repaste）**:
用户从历史中选用一条剪贴板条目：先将其写回系统剪贴板，再触发一次粘贴；完成后关闭历史面板。写回导致的剪贴板变化按连续去重处理（刷新为最新，不另建条目）。
_Avoid_: 粘贴、恢复、选用、复用

**删除条目（Delete Entry）**:
从剪贴板历史中移除一条指定的剪贴板条目；不影响系统剪贴板。
_Avoid_: 清除、去掉

**清空历史（Clear History）**:
一次性移除剪贴板历史中的全部条目；不影响系统剪贴板。
_Avoid_: 清除、重置、一键清除
